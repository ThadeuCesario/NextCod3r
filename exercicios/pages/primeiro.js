/**
 * O Next utilza o node
 * 
 * Quando criamos arquivos dentro da pasta pages, automaticamente é criado a rota.
 * Portanto nesse caso teremo "url|localhost:3000/primeiro"
 */

function primeiro() {
    return "Primeiro alterado"
}

export default primeiro;